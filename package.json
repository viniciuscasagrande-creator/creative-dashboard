{
  "name": "creative-dashboard",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/run_all_phase_tests.js",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^8.1.4"
  }
}
