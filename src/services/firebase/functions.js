// Cloud Functions module placeholder
export const callFunction = async (name, data) => {
  console.log("Functions: calling simulated function", name, "with data", data);
  return { success: true, result: "mock-result" };
};
